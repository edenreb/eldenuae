---
// Project photo inside an article. Breaks out of the 720px reading column
// on wider screens — a photo held to text measure reads as an illustration;
// one that runs wider reads as the work.
import { Picture } from "astro:assets";
interface Props {
  src: ImageMetadata;
  alt: string;
  caption?: string;
  href?: string;
}
const { src, alt, caption, href } = Astro.props;
---

<figure class="mt-[56px] mb-[56px] md:-mx-[120px]">
  <div class="aspect-[3/2] w-full overflow-hidden bg-ink">
    <Picture
      src={src}
      alt={alt}
      formats={["avif", "webp"]}
      widths={[600, 960, 1400]}
      sizes="(min-width: 768px) 960px, 100vw"
      loading="lazy"
      class="h-full w-full object-cover"
    />
  </div>
  {
    caption && (
      <figcaption class="meta mt-[12px] text-muted">
        {href ? (
          <a href={href} class="underline-offset-4 hover:text-ink hover:underline">
            {caption}
          </a>
        ) : (
          caption
        )}
      </figcaption>
    )
  }
</figure>
